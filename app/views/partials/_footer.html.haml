%div.footer(role="contentinfo")
  %p
    = succeed "," do
      %b= t('author.name')
    2007&ndash;#{Quails::CURRENT_YEAR}
    %br
    %em= t('license.text').html_safe

  = render_service_code(Birdingtop)
