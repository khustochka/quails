-# frozen_string_literal: true
%figure.imageholder
  = link_to image_url(image) do
    = image_tag(jpg_url(image))
  %figcaption.imagetitle
    = link_to image_url(image) do
      = image.decorated.title
