- @page_title = [t('date.standalone_month_names')[@month.to_i], link_to(@year, year_path(@year))].join(' ').html_safe

%h1.over-header= @page_title

= render @posts

%nav.month-navigation
  = link_to "<< #{t('.previous')}", month_path(@prev_month) unless @prev_month.nil?
  = link_to "#{t('.next')} >>", month_path(@next_month) unless @next_month.nil?