-# frozen_string_literal: true
- total_count = @lifelist.size

%ol.lifelist-entries{reversed: true, start: total_count}
  = render partial: "lifer", collection: @lifelist.to_a, locals: {total_count: total_count}
