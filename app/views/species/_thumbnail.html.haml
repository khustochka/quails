- alt_text = "%s (%s)" % [thumbnail.name, thumbnail.name_sci]

%div.imageholder
  = link_to image_tag(thumbnail_url(thumbnail.image), alt: alt_text), species_path(thumbnail)
  %div.imagetitle
    = link_to species_path(thumbnail) do
      %div= thumbnail.name
      %div= name_sci(thumbnail)
