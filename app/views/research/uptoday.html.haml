- @page_title = 'Number of species up to day'

%h1= research_page_header

%h2= l(@today, format: :long)

%dl
  - @uptoday.each do |year, number|
    %dt
      %b= link_to(year, list_path(year: year))
    %dd= number


