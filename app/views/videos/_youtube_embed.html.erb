<%# Be aware this can be rendered outside of the view, be careful with using view helpers, e.g. use I18n.t, not just `t` %>
<iframe title="<%= I18n.t("videos.iframe_title", title: embed.title) %>"
        width="<%= embed.width %>" height="<%= embed.height %>"
        src="<%= embed.url %>" frameborder="0" allowfullscreen></iframe>

