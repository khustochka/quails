-# frozen_string_literal: true
-# Intentionally not wide. Looks better with larger images (2 in a row)
- @page_title = t('.title')

- content_for :translated do
  = render partial: 'partials/translated'

%div.header-with-subheader
  %h1= page_header
  %ul.subheader-menu
    %li= link_to t('images.index.species_gallery'), gallery_path

%div.thumbnails
  = render_justified @images.map(&:to_thumbnail)
