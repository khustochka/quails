- javascript 'ujs'

- @page_title = 'Authorities'

%h1= page_header

= render 'admin/shortcuts' if current_user.admin?

%table.neat_table.loci_index
  %tr
    %th Slug
    %th Name
    %th Description
    %th Species
    %th

  - @authorities.each do |author|
    %tr
      %td
        %b= author.slug
      %td= link_to author.name, authority_path(author)
      %td
        %i
          %small= author.description
      %td.align-center= author.book_species.count
      %td= default_destroy_link(author)
