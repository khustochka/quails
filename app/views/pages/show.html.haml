- stylesheet 'public'

= render 'admin/shortcuts' if current_user.admin?

- @social_likes = @page.likable?
- @canonical = show_page_url(@page)

- cache @page do

  %h1= page_header

  - if @page.likable?
    = render 'social/buttons'

  = @page.formatted.text
