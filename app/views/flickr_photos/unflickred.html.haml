-# @body_class = 'wider'

- stylesheet 'public'
- javascript 'pjax_pagination'

- @page_title = 'Unflickred photos'

= render 'collection_shortcuts'

%h1= page_header

%div.thumbnails
  = render_justified @images.map {|im| Thumbnail.new(flickr_photo_path(im), im.formatted.title, im)}

= paginate @images
