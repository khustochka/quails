- i = 0
- @lifelist.group_by_family.each do |fam, species_list|
  %tr
    %td
    %td{:colspan => 5}
      %h3= "#{fam[:order].upcase}: #{fam[:family]}"
  - species_list.each do |sp|
    %tr
      %td.align-right= "#{i += 1}."
      = render partial: 'lists/advanced/lifer', object: sp
