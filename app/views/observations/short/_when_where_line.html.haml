= observation.observ_date
= surround("(", ")") { observation.card.start_time } if observation.card.start_time.present?
%b= observation.locus.name_en
- if observation.private_notes.present?
  %div= observation.private_notes
