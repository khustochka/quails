- @page_title = 'Species seen after more than a year'

%h2= @page_title

%table.neat_table
  %th Species
  %th Last seen
  %th Seen again
  %th Days

  - @list.each do |el|
    %tr
      %td= species_link(el[:sp])
      %td= el[:date1]
      %td= el[:date2]
      %td.align-right= el[:days]