name: "Build image"
on:
  workflow_call:
    inputs:
      debug:
        default: false
        required: false
        type: boolean
      image_registry:
        type: string
        default: public.ecr.aws
    secrets:
      AWS_ROLE_ARN:
        required: true
    outputs:
      image_tag:
        description: "Docker image tag"
        value: ${{ jobs.merge.outputs.image_tag }}
  workflow_dispatch:
    inputs:
      debug:
        default: false
        required: false
        description: "Install convenience packages (psql etc)"
        type: boolean
      image_registry:
        description: Image Registry
        type: choice
        default: public.ecr.aws
        options:
          - public.ecr.aws
          - ghcr.io
env:
  REGISTRY_IMAGE: ${{ inputs.image_registry == 'ghcr.io' && 'ghcr.io/khustochka/quails' || 'public.ecr.aws/m7x1i1o0/quails' }}
  AWS_REGION: us-east-1
  TAG_SUFFIX: ${{ inputs.debug && '-debug' || '' }}
concurrency:
  group: build-${{ github.ref }}-${{ inputs.image_registry }}
  cancel-in-progress: false
jobs:
  build:
    name: "Build and push"
    strategy:
      fail-fast: false
      matrix:
        arch:
          - name: "AMD64"
            platform: linux/amd64
            runner: ubuntu-24.04
          - name: "ARM64"
            platform: linux/arm64
            runner: ubuntu-24.04-arm
    runs-on: ${{ matrix.arch.runner }}
    permissions:
      packages: write
      contents: read
      attestations: write
      id-token: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
      - name: Prepare
        run: |
          platform=${{ matrix.arch.platform }}
          echo "PLATFORM_PAIR=${platform//\//-}" >> $GITHUB_ENV
      - name: Docker meta
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: |
            ${{ env.REGISTRY_IMAGE }}
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4
      - name: Configure AWS credentials
        if: ${{ inputs.image_registry == 'public.ecr.aws' }}
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}
      - name: Login to AWS ECR Registry
        if: ${{ inputs.image_registry == 'public.ecr.aws' }}
        uses: aws-actions/amazon-ecr-login@v2
        with:
          registry-type: public
      - name: Login to Github Package Registry
        if: ${{ inputs.image_registry == 'ghcr.io' }}
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build and push
        id: push
        uses: docker/build-push-action@v7
        with:
          # Path context is required for .dockerignore to work.
          context: .
          platforms: ${{ matrix.arch.platform }}
          build-args: |
            GIT_REVISION=${{ github.sha }}
            GIT_REPOSITORY_URL=${{ format('{0}/{1}', github.server_url, github.repository) }}
          push: true
          tags: ${{ env.REGISTRY_IMAGE }}
          labels: ${{ steps.meta.outputs.labels }}
          annotations: ${{ steps.meta.outputs.annotations }}
          cache-from: type=gha,scope=${{ matrix.arch.name }}
          cache-to: type=gha,mode=max,scope=${{ matrix.arch.name }}
          outputs: type=image,push-by-digest=true,name-canonical=true,push=true
      - name: Generate artifact attestation
        uses: actions/attest-build-provenance@v4
        with:
          subject-name: ${{ env.REGISTRY_IMAGE }}
          subject-digest: ${{ steps.push.outputs.digest }}
          push-to-registry: true
      - name: Export digest
        run: |
          mkdir -p ${{ runner.temp }}/digests
          digest="${{ steps.push.outputs.digest }}"
          touch "${{ runner.temp }}/digests/${digest#sha256:}"
      - name: Upload digest
        uses: actions/upload-artifact@v7
        with:
          name: digests-${{ env.PLATFORM_PAIR }}
          path: ${{ runner.temp }}/digests/*
          if-no-files-found: error
          retention-days: 1
  merge:
    runs-on: ubuntu-latest
    needs:
      - build
    permissions:
      packages: write
      contents: read
      id-token: write
    outputs:
      image_tag: ${{ steps.result.outputs.image_tag }}
    steps:
      - name: Download digests
        uses: actions/download-artifact@v8
        with:
          path: ${{ runner.temp }}/digests
          pattern: digests-*
          merge-multiple: true
      - name: Configure AWS credentials
        if: ${{ inputs.image_registry == 'public.ecr.aws' }}
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}
      - name: Login to AWS ECR Registry
        if: ${{ inputs.image_registry == 'public.ecr.aws' }}
        uses: aws-actions/amazon-ecr-login@v2
        with:
          registry-type: public
      - name: Login to Github Package Registry
        if: ${{ inputs.image_registry == 'ghcr.io' }}
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Docker meta
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: |
            ${{ env.REGISTRY_IMAGE }}
          tags: |
            type=raw,value=latest,suffix=${{ env.TAG_SUFFIX }},enable=${{ github.ref == format('refs/heads/{0}', 'main') }}
            type=raw,priority=1000,value={{tag}},suffix=${{ env.TAG_SUFFIX }},enable=${{ startsWith(github.ref, 'refs/tags/') }}
            type=sha,priority=800,prefix={{branch}}-,suffix=${{ env.TAG_SUFFIX }},enable=${{ startsWith(github.ref, 'refs/heads/') }}
      - name: Create manifest list and push
        working-directory: ${{ runner.temp }}/digests
        run: |
          docker buildx imagetools create $(jq -cr '.tags | map("-t " + .) | join(" ")' <<< "$DOCKER_METADATA_OUTPUT_JSON") \
            $(printf '${{ env.REGISTRY_IMAGE }}@sha256:%s ' *)
      - name: Inspect image
        run: |
          docker buildx imagetools inspect ${{ env.REGISTRY_IMAGE }}:${{ steps.meta.outputs.version }}
      - name: Outputs
        id: result
        run: echo image_tag=${{ fromJSON(steps.meta.outputs.json).tags[0] }} >> "$GITHUB_OUTPUT"
        shell: bash
