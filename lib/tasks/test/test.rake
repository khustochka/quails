# This is supposed to run system tests with the whole suite. I have moved system tests into integration for now.
# Still may need to use this in the future.
#
# task :test do
#
#   Rake::Task["test:system"].invoke
#
# end
#
# # FIXME: `bin/rake test` runs all, but `bin/rails test` does not
# # for this use `bin/rails test:all`
#
# namespace :test do
#
#   task :all => [:test]
#
# end
